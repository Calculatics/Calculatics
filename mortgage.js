const $=id=>document.getElementById(id);const money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});function calc(){const price=+$('price').value||0,down=+$('down').value||0,rate=(+$('rate').value||0)/100/12,years=+$('term').value||30,tax=(+$('tax').value||0)/12,ins=(+$('insurance').value||0)/12,loan=Math.max(price-down,0),n=years*12;let pi=0;if(loan&&rate)pi=loan*rate*Math.pow(1+rate,n)/(Math.pow(1+rate,n)-1);else if(loan)pi=loan/n;const interest=Math.max(pi*n-loan,0);$('monthly').textContent=money.format(pi+tax+ins);$('pi').textContent=money.format(pi);$('taxMonthly').textContent=money.format(tax);$('insMonthly').textContent=money.format(ins);$('interest').textContent=money.format(interest);$('loan').textContent=money.format(loan)}document.querySelectorAll('input,select').forEach(x=>x.addEventListener('input',calc));calc();
